# v1.4.0 Release Preparation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Finalize the current Zig SDK refactor as `v1.4.0`, update release metadata, verify the release candidate, and publish the release.

**Architecture:** Treat this as a minor release because the changes are additive and compatibility-preserving but materially expand and reorganize the public SDK surface. Update package metadata and release documentation, verify the release candidate, then commit, tag, push, and publish the GitHub release.

**Tech Stack:** Zig, git, GitHub CLI, existing CI workflow

### Task 1: Update release metadata

**Files:**
- Modify: `build.zig.zon`
- Modify: `CHANGELOG.md`
- Modify: `README.md`
- Modify: `docs/TROUBLESHOOTING.md`

**Step 1: Bump version**

Set package version to `1.4.0`.

**Step 2: Write release notes**

Move the current completed work into a new `## 1.4.0 - 2026-03-17` changelog section.

**Step 3: Update versioned docs**

Update hard-coded release references and package-fetch examples from `v1.3.1` to `v1.4.0`.

### Task 2: Verify the release candidate

**Files:**
- Test only

**Step 1: Run verification**

Run:
- `zig build rpc-test --summary all`
- `zig build test`
- `zig build examples`
- `zig build -Doptimize=ReleaseFast`

Expected: PASS

### Task 3: Integrate and publish

**Files:**
- VCS/release only

**Step 1: Commit**

Commit the release candidate changes on `main`.

**Step 2: Tag**

Create annotated tag `v1.4.0`.

**Step 3: Push**

Push `main` and `v1.4.0` to `origin`.

**Step 4: Publish release**

Create the GitHub release for `v1.4.0` using the changelog summary.
